Adding LetsEncrypt root certificates to the Java keystore on Windows.

Clone (or download and unpack zip archive), open windows console, change
directory to project directory and run "run.bat" file as administrator. Please
ensure you have Sun JDK installed and JAVA_HOME environment variable is set.

Inspired by http://www.ailis.de/~k/uploads/scripts/import-startssl 
